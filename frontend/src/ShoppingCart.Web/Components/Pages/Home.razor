@page "/"
@inject NavigationManager Nav

@code {
    //protected override void OnInitialized() => Nav.NavigateTo("/products");

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            Nav.NavigateTo("/products");
        }
    }
}
